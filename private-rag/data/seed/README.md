# 种子知识库目录

把任意 `.pdf` / `.docx` / `.txt` / `.md` 文件丢进这个目录，**首次启动会自动入库**，
无需手动上传。已经在库中的文件会被跳过（按文件内容 hash 判定）。

```bash
# 例如
cp ~/我的产品手册.pdf data/seed/
python run.py
```

也可以完全不用这个目录 —— 直接启动后在页面上点「上传文档」即可。

> 说明：本仓库默认**不包含**任何真实业务文档。根目录下若存在 `品牌手册1.docx`
> 也会被自动入库，该文件已在 `.gitignore` 中排除。
