# resume_show — 项目集工作区
#
# 各项目的 .gitignore 在子目录内独立生效（凭据、构建产物等）。
# 这里只处理工作区级别的忽略规则。

# 归档目录：旧版本、第三方素材、各项目的历史 .git，不发布
_archive/

# 本地工作区工具（可能含密钥检测规则，不入库）
push-to-github.ps1

# 临时产物
Thumbs.db
desktop.ini
.DS_Store
*.tmp
